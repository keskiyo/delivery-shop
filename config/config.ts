// Экспортируемый объект с конфигурацией приложения
// Используется во всем проекте для получения настроек: пагинации, скидок, бонусов, лимитов
export const CONFIG = {
	// === ПАГИНАЦИЯ: ТОВАРЫ ===

	// Количество товаров на странице поиска или фильтрации (страница "Найденные товары")
	ITEMS_PER_PAGE: 3,

	// Количество товаров в блоке "хиты продаж" / "рекомендуемые" на главной странице
	ITEMS_PER_PAGE_MAIN_PRODUCTS: 4,

	// Количество статей в блоке "новости" на главной странице
	ITEMS_PER_PAGE_MAIN_ARTICLES: 3,

	// Количество товаров на странице категории каталога (страница категории товаров)
	ITEMS_PER_PAGE_CATEGORY: 6,

	// Количество заказов на одной странице в истории заказов пользователя
	ITEMS_PER_ORDERS_PAGE: 4,

	// === ПАГИНАЦИЯ: СТАТЬИ/БЛОГ ===

	// Количество статей на странице списка всех статей блога
	ARTICLES_PER_BLOG_PAGE: 3,

	// Количество статей для отображения в блоке "похожие статьи" внизу страницы статьи
	ARTICLES_PER_ARTICLE_PAGE: 3,

	// Количество комментариев на странице статьи (пагинация комментариев)
	COMMENTS_PER_ARTICLE_PAGE: 5,

	// === СКИДКИ И БОНУСЫ ===

	// Процент скидки по карте лояльности (дополнительно к скидке товара)
	// Если у пользователя есть карта, он получает +6% скидку
	CARD_DISCOUNT_PERCENT: 6,

	// Процент бонусов, начисляемых от суммы заказа
	// При покупке начисляется 5% от потраченной суммы в виде бонусов
	BONUSES_PERCENT: 5,

	// Максимальный процент от суммы заказа, который можно оплатить бонусами
	// Ограничение: не более 10% от итоговой суммы можно покрыть бонусами
	// Пример: заказ на 1000₽ -> максимум 100₽ можно оплатить бонусами
	MAX_BONUSES_PERCENT: 10,

	// === ЦЕНЫ ===

	// Диапазон цен по умолчанию в фильтре, если пользователь не задал свои значения
	// Используется на странице каталога и в фильтрах товаров
	FALLBACK_PRICE_RANGE: { min: 0, max: 3000 },

	// Минимальная сумма заказа в рублях
	// Покупатель не может оформить заказ, если итоговая сумма меньше 1000₽
	// Проверяется в хуке usePricing.ts (isMinimumReached)
	MIN_ORDER_PRICE: 1000,

	// === САЙТ ===

	// URL основного домена сайта
	// Используется для формирования ссылок в письмах, SEO-тегов, редиректов
	SITE_URL: 'https://delivery-shop.ru',

	// Домен для временных email при регистрации через телефон
	// При регистрации по телефону создается email: 79001234567@delivery-shop.ru
	// Это позволяет использовать phone как уникальный идентификатор в better-auth
	TEMPORARY_EMAIL_DOMAIN: '@delivery-shop.ru',

	// === БЕЗОПАСНОСТЬ: OTP ===

	// Максимальное количество попыток ввода OTP-кода подтверждения телефона
	// После 3 неудачных попыток нужно запрашивать новый код
	// Настраивается в плагине phoneNumber в auth.ts
	MAX_ATTEMPTS: 3,

	// Время жизни OTP-кода в секундах (180с = 3 минуты)
	// По истечении этого времени код становится недействительным
	// Пользователь должен запросить новый код
	// Настраивается в плагине phoneNumber в auth.ts
	TIMEOUT_PERIOD: 180,

	// === ПРОЧЕЕ ===

	// Размер страницы по умолчанию для универсальных списков
	// Используется в админ-панели для таблиц, генера списков
	DEFAULT_PAGE_SIZE: 5,
}
